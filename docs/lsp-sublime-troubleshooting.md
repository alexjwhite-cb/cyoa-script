# Troubleshooting LSP in ST3

## Minimal test config

If the LSP log is completely empty (no "Starting client", no errors),
the configuration is likely not being loaded or matched. Try this minimal config:
