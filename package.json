{
  "name": "cyoa-docs-site",
  "version": "0.6.0",
  "description": "Documentation site builder for the CYOA Engine (uses marked for markdown rendering)",
  "private": true,
  "dependencies": {
    "marked": "^12.0.0"
  }
}
